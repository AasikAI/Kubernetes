apiVersion: v1
kind: Service
metadata:
  name: service
  labels:
    app: network
spec:
  type: NodePort
  selector:
    app: springpod
  ports:
    - port: 80
      targetPort: 8080
      nodePort: 30003 
