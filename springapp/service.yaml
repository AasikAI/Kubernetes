apiVersion: v1
kind: Service
metadata:
  name: spring
  labels:
    app: spring
spec:
  type: NodePort
  selector:
      app: spring-label
  ports:
    - port: 8080
      targetPort: 8080
      nodePort: 30005 
